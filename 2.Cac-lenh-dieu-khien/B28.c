/*Viết chương trình tính tiền karaoke theo cách sau:
THỜI GIAN			ĐƠN GIÁ / GIỜ
...<- 10h			Giờ nghỉ
10h -> 17h			20.000 đồng
17hh -> 24h			45.000 đồng
24h ->...			Giờ nghỉ */

#include <stdio.h>
#include <conio.h>

void main()
{
    // int gv, gr;
    // float tt = 0;

    // printf("Nhap gio vao: ");
    // scanf("%d", &gv);
    // printf("Nhap gio ra: ");
    // scanf("%d", &gr);

    // if (gv >= 10 && gv < 17 && gr < 17)
    //     tt = (gr - gv) * 20000;
    // else if (gv >= 17 && gv < 24 && gr < 24)
    //     tt = (float)(gr - gv) * 45000;

    // printf("Thanh tien: %.2f", tt);
    getch();
}